// import { useEffect } from "react";
// import { HeaderSettings } from "../types";
// // import { applyCSSVariables } from "../cssUtils";

// /**
//  * Custom hook to apply CSS variables when settings change
//  * @param settings The header settings object
//  */
// export const useCssVariables = (settings: HeaderSettings): void => {
//   useEffect(() => {
//     if (settings.logo) {
//       console.log("Logo settings in Header:", settings.logo);
//     }

//     // Apply CSS variables whenever settings change
//     // applyCSSVariables(settings);
//   }, [settings]);
// };
